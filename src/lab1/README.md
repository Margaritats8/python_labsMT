 ## Это моя первая лаба

# Задание 1

##  На вход подаются имя (формат строки) и возраст (формат целых чисел). Далее при помощи f-строк выводится приветствие.
![вывод 1](https://github.com/Margaritats8/python_labsMT/blob/main/img/img1/task1.png)


# Задание 2

## На вход подаются 2 строки, при помощи метода replace() данные приводятся к нужному виду и переводятся в формат дробных чисел. Ну а дальше просто выводится сумма и среднее арифметическое, с помощью f-строк округлённые до 2х знаков после запятой.
![вывод 2](https://github.com/Margaritats8/python_labsMT/blob/main/img/img1/task2.png)

# Задание 3

## На вход подаётся 3 дробных числа, которые обрабатываются по заданным формулам. Полученный результат выводится округлённый до 2х знаков после запятой (.2f) и сдвинутый на n знаков вправо (>n) для красивого результата.
![вывод 3](https://github.com/Margaritats8/python_labsMT/blob/main/img/img1/task3.png)


# Задание 4

## На вход подаётся кол-во минут. Кол-во часов и кол-во оставшихся минут вычисляется по базовым формулам. Далее данные с помощью f-строк выводятся с добавлением до 2х знаков справа, чтоб получился формат ЧЧ:ММ (:02d). Также добавила в код (hours % 24), чтоб, если количество минут превышало сутки, отсчёт начинался сначала.
![вывод 4](https://github.com/Margaritats8/python_labsMT/blob/main/img/img1/task4.png)

# Задание 5

## На вход подаётся строка, которая разделяется по пробелам и получается список, игнорирующий лишние пробелы. Далее для получения инициалов в каждом элементе списка берётся его нулевой индекс. Ну а далее просто проходимся по элементам списка и добавляем их длину в соответственную переменную. При выводе обязательно добавляем к получившейся длине 2, потому что при подсчёте кол-во символом не учитываем пробелы между 1ым и 2ым словом и между 2ым и 3ым словом.
![вывод 5](https://github.com/Margaritats8/python_labsMT/blob/main/img/img1/task5.png)

# Задание 6

## Создаём переменные, считающие очников и заочников, ну и вводим количество запросов. Далее с помощью цикла проходимся от 1 до n и с каждой итерацией проверяем нового участника на соответствие требований для ввода (ФИ возраст и T/F). Если на вход подали некорректные данные, просто игнорируем этого пользователя. Если всё хорошо, проверяем последний элемент поделённой строки и добавляем в соответсвтв. счётчик. Ну и в конце вывод, конечно.
![вывод 6](https://github.com/Margaritats8/python_labsMT/blob/main/img/img1/task6.png)

# Задание 7

## Создаём переменные, с помощью которых потом будем определять расстояние между элементами, и просим пользователя ввести зашифрованную строку. Далее проходимся по каждому элементу строки и ищем первую заглавную букву, далее прерываем цикл. Потом аналогично ищем первую цифру и считаем разницу между найденными индексами. Найдя необходимые данные проходимся по оставшейся строке, начиная уже с 3 элемента в загаданном слове и собираем всё слово до конца. И если нет точки в конце, добавляем. Вуаля!
![вывод 7](https://github.com/Margaritats8/python_labsMT/blob/main/img/img1/task7.png)

